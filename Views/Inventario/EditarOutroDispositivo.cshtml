@model ChamadosTI.Models.InventarioOutrosDispositivoFormViewModel
@{
    ViewData["Title"] = "Editar dispositivo";
}

<section class="page">
    <div class="page-header">
        <div>
            <p class="eyebrow">Inventário</p>
            <h1>Editar dispositivo</h1>
        </div>
    </div>

    <form asp-action="EditarOutroDispositivo" asp-route-id="@Model.Id" method="post" class="card form-grid" data-inventario-form>
        @Html.AntiForgeryToken()
        <partial name="_OutroDispositivoForm" model="Model" />
        <div class="form-actions">
            <button type="submit" class="btn primary">Salvar alterações</button>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
